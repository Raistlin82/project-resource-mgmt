# UX residual triage — 2026-08-05

Verified against `main` at `33c22fd`. 0 issues triaged: **41 still real**, 6 already fixed incidentally.

Each finding's `evidence` was read from the tree at triage time, not from the register.

## Open product decisions

- P1-09 — /reporting is a PM/RM function but its load pulls commercial and financial datasets those roles may not read. The remedies differ in what users see and who sees it: split datasets per capability, project a portfolio endpoint down to authorised fields, or narrow the audience of /reporting itself. Not a defect to fix blind — decide the audience first (register line 29, src/app/reporting/reporting.ts).
- REVIEW-§1.3 (no issue-list id) — src/server.ts:6419 loadFinanceData() passes raw repos.resources.list() rows into the server-side export envelope, whose billRate is the per-resource override in €/DAY, so the un-negotiated as-incurred figures in the GL journal, BI feed and e-invoice artifacts overstate by hoursPerDay (the negotiated path in the same envelope converts correctly). Recorded as a KNOWN, ADJUDICATED INCONSISTENCY attributed to a user decision and pre-existing on main. Restating it as a defect would change amounts already emitted to downstream systems — the user must decide whether the exports change unit or the comment stands.

## Batches (18)

### B1. Read-state contract: one three-valued gate per screen

- **effort:** large
- **issues:** P1-03, P2-02, P3-03, P2-34
- **why together:** P1-03's shared resourceState helper is the decision the other three consume. P2-02 is that gate applied to contract-details' single top-level @if; P3-03 is the LOADING BRANCH of the same gates (seven hand-rolled regions that announce nothing because aria-label on a role=generic div is not an accessible name); P2-34's residual is literally the same lines — hoisting the <h1> above project-details' @if and demoting contract-details' bespoke <h3> not-found card, which this batch rewrites anyway. /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/commercial/contract-details/contract-details.ts and .../projects/project-details/project-details.ts appear in all four. Where P1-03 and P3-03 contradict each other — P1-03 says route loading/error through app-list-state, P3-03 says its three skeleton shapes cannot express a 6-tile KPI grid — believe P3-03: keep app-list-state for list/table surfaces and for the error branch, and for the KPI/money grids either add a projection/skeleton="none" mode to ListStateComponent or replicate its exact ARIA contract (role=status + aria-live=polite + aria-busy + sr-only label). Do not force the visuals through the existing shapes.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/services/auth-gated-resource.util.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/shared/list-state.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/commercial/contract-details/contract-details.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/project-details/project-details.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/commercial/contracts/contracts.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/commercial/orders/orders.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/resource-requests/resource-requests.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/configuration/manage-locations.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/reporting/reporting.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/dashboard/dashboard.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/app.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/scripts/smoke-noauth.mjs
- **risk:** Turning .value() reads into gated content can blank a screen that works today, and rewriting app.ts:566-581's catchError changes visible nav badge counts. Existing detectors: contract-details.spec.ts's money-region block (~:369-509, which already stubs a FORBIDDEN read) and bench.component.spec.ts:187's [role=status][aria-busy=true] assertion. For the other ~30 screens NO existing test would notice either the fix or a regression — the only scaling detector is a new static gate beside ungatedReads() in scripts/smoke-noauth.mjs (a file with an rxResource + template must reference [error]= or a State()==='error' branch), which has to land in the same PR behind a shrinking allow-list or it can never go green. scripts/smoke-noauth.mjs:33-44 records that its SSR pass cannot see this class at all: a screen that handles the error renders byte-identically to one that does not.

### B2. App shell: nav accordion, ⌘K, mobile drawer

- **effort:** small
- **issues:** P2-05, P2-06, P1-23
- **why together:** Three edits to one file and to the same two elements — the <aside id="primary-navigation"> and its group bodies. P2-05 and P1-23 both collide with the same structural fact: one element is both the mobile drawer and the desktop sidebar, and both reach for inert/visibility on it (a blanket [inert] would kill desktop nav, which is why P1-23 was left). P2-06's ⌘K has to open whichever container P1-23 has just made non-focusable, and focus a frame later. The three must agree on one open/hidden model; separate passes conflict line-for-line in app.ts:107-210.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/app.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/styles.css, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/app.spec.ts
- **risk:** A breakpoint-blind visibility/inert rule on the shared aside strands desktop navigation entirely; a same-tick focus after opening the drawer silently no-ops while looking correct. app.spec.ts:131-139 (main[inert] when the drawer is open) and :143-156 (accordion aria-controls resolve) would notice a wiring regression; nothing today covers ⌘K, the active-group reopen, or the closed drawer's tab order. Two honest limits to state: the ⌘K handler is registered inside afterNextRender, so the new spec must first prove the baseline fires or every later assertion is vacuous; and jsdom applies no stylesheet and evaluates no media query, so P1-23's off-screen-links-leave-the-tab-order behaviour can only be asserted as a data-attribute marker plus stylesheet text, not as behaviour. app.ts and styles.css are also edited by B3, B6 — land this batch first.

### B3. Error surfaces: one owner, one persistent boundary

- **effort:** medium
- **issues:** P1-30, P2-29
- **why together:** Both decide who owns the visible error. P2-29's residual is two owners toasting one failure (errorInterceptor:65 plus six component handlers, which the message-keyed dedup at notification.service.ts:34 cannot collapse); P1-30's is that the only feedback now expires after 12s, leaving a blank or half-rendered route with no message and no control. Both edit notification.service.ts / error.interceptor.ts and both must respect the project-wide decision that error toasts DO auto-dismiss — so the persistent affordance has to be the route-level boundary, not a sticky toast. Settling that once avoids two conflicting conventions in the same two files.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/services/global-error-handler.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/app.config.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/app.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/services/notification.service.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/interceptors/error.interceptor.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/utilization/utilization.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/approvals/approvals.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/allocation-approvals/approval-modal.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/configuration/integrations.component.ts
- **risk:** A boundary rendered BESIDE the outlet instead of in place of it leaves the user staring at the wreckage with an alert on top — the load-bearing assertions are router-outlet absent when failed and present when not. An HttpContext suppression applied too broadly silences real failures, so the mirror case (no token → the interceptor message DOES appear) is mandatory. notification.service.spec.ts:33-45,:65-85 would notice a dedup/cap/auto-dismiss regression and global-error-handler.spec.ts would notice broken wiring; nothing today asserts single ownership — that test must compare the FULL message string, since both the interceptor's and the component's text contain 'Failed'. Touches app.ts (B2) and approval-modal.component.ts (B7).

### B4. Auth bootstrap: demo mode, HTTPS policy, JWT decode

- **effort:** medium
- **issues:** P2-30, P2-32, P1-28, P2-31
- **why together:** All four rewrite the same 50 lines of auth.service.ts (decodeJwtPayload :54-70, init :190-244) and must agree on ONE branch table: demoMode true → skip discovery and applyDemoAdmin (P2-32); demoMode false → configure OIDC with an issuer-derived requireHttps (P1-28) and a byte-correct UTF-8 claim decode (P2-30). P2-31 is the documented entry point into that same table (README/CLAUDE.md/package.json quick start plus the missing 'identity provider unreachable' surface for the plain-ng-serve dead end). Three separate passes would each invalidate the others' assumptions.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/services/auth.service.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/services/auth-runtime.util.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/services/auth-runtime.util.spec.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/app.config.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/README.md, /Users/gabriele.rendina/tools/project-resource-mgmt/CLAUDE.md, /Users/gabriele.rendina/tools/project-resource-mgmt/package.json, /Users/gabriele.rendina/tools/project-resource-mgmt/.claude/launch.json
- **risk:** Highest blast radius here: a wrong branch either breaks sign-in for every real deployment or silently keeps permitting a plain-http token flow, which is P1-28's whole point. There is no auth.service.spec.ts — only auth-runtime.util.spec.ts would notice, and only for pure functions, which is exactly why requireHttpsFor must live in auth-runtime.util.ts and be asserted with toBe('remoteOnly')/toBe(true), plus one configure() spy assertion so a correct pure function cannot sit unused beside a hardcoded false. Do NOT flip AUTH_TRUST_HEADERS' default at src/server.ts:542: scripts/smoke-noauth.mjs is the gate that proves reads are deny-by-default and is the one check without a blind spot here. The UTF-8 fixture must be base64url-encoded from real UTF-8 bytes and asserted with strict equality — 'ZoÃ« MÃ¼ller' contains 'Zo'. Note the 127.0.0.1 caveat: the library's http allowance is a literal localhost regex.

### B5. Table markup sweep: scroll containers, captions, header/row scope

- **effort:** large
- **issues:** P1-25, P2-28
- **why together:** Both rewrite the same <table class="command-data-table"> blocks in the same ~41 files — resources.component.ts and the seven configuration manage-* screens are named by both — and both are enforced by the same kind of source-level guard. One pass per feature area edits each table once; two passes re-touch and re-review every one of them. Shared decision: keep command-data-table and styles.css:1064's nowrap (correct once the container scrolls) and add the wrapper + caption + scope, rather than inventing role="table" ARIA over native markup.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/resources/resources.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/configuration/manage-rate-cards.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/configuration/manage-locations.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/configuration/manage-vendors.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/configuration/manage-cost-centers.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/project-tasks/project-tasks.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/capacity/capacity.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/styles.css
- **risk:** A scroll wrapper inside a card changes the card's overflow/rounded-corner behaviour, and promoting a row's first <td> to <th scope="row"> re-styles it through styles.css:1064's nowrap and any first-child selectors. jsdom performs no layout, so NO check in this repo — existing or new — can prove the clipped Actions column is reachable at 390px; say that plainly. The honest detectors are two static guards (every file containing command-data-table also contains overflow-x-auto; every <table> block has a caption or aria-label and every <th> a scope) which must start red on ~9 and ~61 tables — keep that first red output as review evidence, because a green start means the extraction regex is broken. Land per feature area, never as one diff; the same nine configuration screens are edited by B6 and contract-details/project-details by B1.

### B6. Contrast tokens and reveal-on-hover class sweep

- **effort:** medium
- **issues:** P2-27, P2-13
- **why together:** Both are class-attribute sweeps over the same action clusters (the seven manage-* delete buttons, resources, projects, project-partners, utilization, reporting, my-profile) plus new declarations in src/styles.css. They share the decision that forces the shape of both fixes: this design system has no dark: variant — theming is :root[data-theme="dark"] token overrides — so the fix must be tokens/variants declared in styles.css (--color-on-accent/--color-on-critical mirroring what --color-ink-inverse already does at :216; a coarse-pointer variant or Tailwind v4's pointer-coarse). Doing them in one pass edits each button's class string once and shares one static-guard harness.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/styles.css, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/app.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/forecast/forecast.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/utilization/utilization.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/resources/resources.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/projects/projects.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/project-partners/project-partners.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/my-profile/my-profile.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/scripts/smoke-noauth.mjs
- **risk:** New tokens change the label colour of every primary and destructive button in dark mode simultaneously; a wrong value makes labels unreadable rather than merely low-contrast, and nothing in the suite asserts contrast today. Land the grep guard FIRST (no element whose class list holds both bg-accent|bg-critical and text-white → the honest red on 15 call sites), then the numeric OKLCH→sRGB→WCAG spec asserting the RATIO >= 4.5 in both themes — never a token name, which is green against today's 3.40:1. For P2-13 state explicitly that no check here can observe 'invisible on a touch tablet' (no layout in jsdom, no browser runner in package.json), so the static class guard is the only detector and a per-component toContain('focus-within:opacity-100') is a class-string tautology. Do not drop the sm: rule without adding the pointer rule, or phones regress. Same nine configuration screens as B5.

### B7. Allocation approval modal: scope-truthful copy and wrapping

- **effort:** small
- **issues:** P2-22, P2-23
- **why together:** One file, and the two fixes edit adjoining regions of it: the project-line row (:168-177) and the footer button strip (:342-357). P2-22 adds a counted label to exactly the buttons P2-23 makes wrap, and both must read the same signal the action reads (checked(), not rows()) — separate passes conflict. Both also use the existing ROW_TWO_PENDING fixture, so one spec pass covers both.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/allocation-approvals/approval-modal.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/allocation-approvals/approval-modal.component.spec.ts
- **risk:** The footer copy is the user's only statement of what an approval will affect; a label wired to the wrong signal restates today's overpromise more confidently, and the modal title (:456-462) carries the same lie in the most prominent position — fix both or the batch is cosmetic. approval-modal.component.spec.ts already has the two-decidable-line fixture but no label assertion; it must use toBe on the trimmed label plus the round-trip back to 'Approve month', because toContain('Approve') passes in every state. For the layout half, jsdom cannot see the overflow — only the structural precondition (flex-wrap on the row and footer, min-w-0 on the label span) is assertable, and the test name must say so. The same footer buttons are edited by B3 and B8: land this batch first.

### B8. Mutation dispatch: submit wiring and in-flight guards

- **effort:** medium
- **issues:** P2-16, P2-17
- **why together:** Both change when and how often a write fires, on overlapping files (change-requests.ts, billing.ts). Wiring Enter-to-submit (P2-16) without an in-flight guard (P2-17) is exactly how a double-create happens, so they must land together and settle one convention: <form id> + type="submit" form=… for footer buttons outside the form, type="button" kept on Cancel, an early if (saving()) return, [disabled] plus a Saving… label, and a reset in BOTH the next and error callbacks. Rank by blast radius, not file count: approval-modal's decide() batch first.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/change-requests/change-requests.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/commercial/billing/billing.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/allocation-approvals/approval-modal.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/project-partners/project-partners.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/my-profile/my-profile.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/my-assignments/my-assignments.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/server.ts
- **risk:** Functionally the riskiest UI batch: a flag reset only in next leaves Save permanently dead after one failure, and a double-fire on approval-modal's decide() re-sends an approval batch. my-assignments.component.spec.ts:242 (plus the error-reset case at :190) is the only existing proof of the pattern and says nothing about the other screens; billing.spec.ts exists but has no submit or duplicate case; change-requests and project-partners have no spec at all, so those must be written or the batch ships unverified. Do not assert type="submit" alone — a submit button not associated with a form still does nothing; dispatch a real submit Event from the form and assert the create spy count, with the invalid-form negative half. If the idempotencyKey extension is taken it edits src/server.ts:2094-2129 (server, SoD/approval paths) and raises review depth accordingly; a client pending flag stops one tab and nothing else.

### B9. Forecast capacity math: null utilization, dated and time-phased supply

- **effort:** medium
- **issues:** P1-17, P1-18
- **why together:** Both rewrite the same CapacityPeriod rows in forecast.util.ts and are coupled by construction: P1-18 moves the supply computation inside the period loop and filters on hire/termination, which is what CREATES the zero-supply periods whose utilizationPct P1-17 must render as 'no answer' instead of a green 0%. They also need the same fixture change (the res() helper cannot express a terminated resource today) and they must agree on one contract before either downstream reader (chart series, average, CSV) is touched.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/services/forecast.util.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/services/forecast.util.spec.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/forecast/forecast.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/forecast/what-if.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/services/capacity.util.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/db/seed.ts
- **risk:** These numbers drive staffing decisions and the /forecast CSV. forecast.util.spec.ts:164-176 currently PINS the defect (utilizationPct expected 0 at zero supply) and must be rewritten — that red is the goal, not a regression. Reimplementing capacity.util.ts's isActiveInMonth on a shared isActiveInWindow can move /bench and /capacity headcounts, which the bench/capacity util specs and src/server/server-logic.spec.ts would notice; leave countsTowardDeliveryCapacity alone (shared with src/server.ts, and its contract is a kind question). Three downstream readers coerce null back to 0 if missed — forecast.ts:422 chart series, :442 average, :489 CSV — plus what-if.ts:902, and none of them has an assertion today. Do not assert the pill by substring: '100%' contains '0%'.

### B10. Bar chart component: honoured height and demand/supply overlay

- **effort:** medium
- **issues:** P2-26, P2-24
- **why together:** Both change the same component's render contract in the same code path — P2-26 makes the figure own its box height (today --ldg-h is written and never read, so five call sites' heights are discarded), P2-24 adds the overlay series plus its contribution to the y-domain. Neither is verifiable without the first spec src/app/shared/charts/ has ever had, and a flex-height figure interacting with a new domain max is one review, not two.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/shared/charts/command-bar-chart.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/forecast/forecast.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/forecast/what-if.ts
- **risk:** Seven call sites re-lay-out at once (forecast, what-if, reporting ×2, dashboard, +2) and an overlay excluded from the domain clips the supply line — the chart then understates capacity while looking correct, which is why the domain-max assertion matters more than the rect-presence one. Nothing guards any chart today. jsdom does not lay out, so the height half needs a real-layout runner or an explicitly-labelled CSS-source assertion: asserting figure.style --ldg-h === '200px' passes against today's bug. Watch two mechanics: .ldg-sr is position:absolute and needs the figure position:relative once it becomes a flex container, and [stacked]=true currently stacks EVERY series, so Supply must be moved out of the series list, not just flagged. forecast.ts/what-if.ts are also edited by B9 — land B9 first.

### B11. Schedule timeline: keyboard reassign, honest role, touch pan, ARIA rows

- **effort:** large
- **issues:** P2-10, P2-11
- **why together:** One 1100-line component with no spec at all; both fixes touch the same markup (the grid at :272-350, the bars at :325-331) and both need the same new harness with two populated lanes (seed assignments '1' and '3'). Batching also forces a decision they share: whether a bar keeps role="button" — which then owes Enter/Space — or the track becomes an application region with a real focusable button per bar, a choice P2-11's row wrappers must be structurally consistent with.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/schedule/schedule.component.ts
- **risk:** P2-10 edits the drag commit path that reassigns an assignment to a different resource — a real mutation — and relaxing touch-action plus adding a movement threshold can break pointer dragging outright. P2-11's display:contents row wrappers can break the sticky lane column and the grid column math, and row exposure under display:contents must be verified in an actual a11y tree rather than assumed (fallback: drop the table pretense and expose lanes as named sections). No spec exists for the component (only src/app/services/schedule.util.spec.ts), so NEITHER regression would be noticed until the new spec is written — treat writing it as part of the batch. The threshold test must assert the pair (3px → zero update calls, 20px → exactly one); a one-sided version passes with no threshold at all. Assert the reassign on the api spy payload, not the DOM, since the bar's position is recomputed either way.

### B12. Local civil date for every 'now'

- **effort:** small
- **issues:** P2-21
- **why together:** One decision applied at the three remaining sites: derive the civil date through the existing localIsoDate/todayLocalIso helper and leave the UTC arithmetic they feed untouched (schedule's Monday anchor, billing's overdue baseline, dashboard's trailing window/chart periods). Deliberately not folded into the schedule or billing batches: what is shared here is the date rule and the TZ-crossing test technique, not the file — and my-assignments.component.ts:555 should import the shared helper instead of duplicating it so there is one definition to audit.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/schedule/schedule.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/commercial/billing/billing.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/dashboard/dashboard.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/services/local-date.util.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/my-assignments/my-assignments.component.ts
- **risk:** billing.ts's value feeds days-overdue on invoiced items, so a wrong conversion shifts an aging figure by a day; finance.util's daysBetween is covered but none of the three call sites is. Every existing suite runs under the machine's TZ, so the new tests must pin TZ and fake the clock at an instant where UTC and local disagree (e.g. 2026-08-03T22:30:00Z under Europe/Rome; 2026-07-31T23:30:00Z under America/New_York) — under TZ=UTC all three cases pass while broken, the exact vacuous green this project keeps hitting. Compare whole date/period strings, not substrings. Small isolated edits inside schedule.component.ts (B11) and billing.ts (B8): rebase, do not merge into those batches.

### B13. Money figures that state the overrun, plus the no-project affordance

- **effort:** small
- **issues:** P1-15, P2-25, P2-18
- **why together:** P1-15 and P2-25 must settle ONE convention for a negative financial result — an explicit label ('Over budget'; a critical-tint overrun segment), the magnitude rendered positive, a distinct colour token as the redundant channel, and never the minus sign or a zero-width segment as the only signal. P2-18 rides along because it edits financial-plans.ts's header/Create control and its spec in the same pass; a second pass over that file and financial-plans.spec.ts would conflict. All three are display/affordance changes with no mutation semantics, so the review depth is uniform.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/financial-plans/financial-plans.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/financial-plans/financial-plans.spec.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/project-details/project-details.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/project-partners/project-partners.ts
- **risk:** financial-plans.spec.ts:54-56 locates the KPI tile by the literal label 'Remaining'; a conditional label breaks that lookup, so the selector must move to a data-testid in the same commit or the suite goes red and invites reverting the label instead of the test. project-details' margin bar has no spec at all and the seed cannot reach the overrun branch (project '1' is comfortably profitable at seed.ts:778-779), so the fixture must be stubbed — assert that the Labor and External class strings DIFFER (toContain('caution') is true of both today), that a critical-tint overrun element exists, and that the segment widths sum to ≤100. For P2-18 assert both halves (button disabled AND the NotificationService spy never called); either alone goes green on a half-fix. Do not add an error→[] accessor or fabricate 0% while fixing the bar — that block's missing load gate is B1's scope. project-details.ts is also edited by B1, B5, B14.

### B14. Project details tabs: URL state, ARIA, capability-checked deep links

- **effort:** large
- **issues:** P2-08
- **why together:** Alone by risk, not by size. The strip needs tablist/tab/aria-selected, roving focus, arrow-key handling and a tab query param — but the panels are keyed on activeTab() alone while the tab LIST is capability-filtered (financials/cost-centers behind canApproveFinancials, rates behind canManageCommercial). So the deep-link feature is one careless line from rendering finance and commercial panels for a role that has no tab for them. That RBAC-adjacent decision sets the review depth and must not travel with heading, colour or table work in the same file.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/project-details/project-details.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/app.routes.ts
- **risk:** A URL value trusted straight into activeTab() is a client-side RBAC leak: the server still gates the data, but the panel chrome, labels and any already-loaded figures are not gated. The falsifiability anchor is the pm + ?tab=financials case asserting activeTab() falls back to 'overview' AND that the 'Actual Cost' label is absent — the tablist/arrow-key/deep-link cases all pass on an implementation that trusts the URL blindly. There is no project-details.spec.ts today, so nothing notices anything on this screen. The file is restructured by B1 (state gate + h1 hoist) and edited by B13/B5 — sequence after B1.

### B15. Form validation feedback: shared field primitive and the missing inline errors

- **effort:** large
- **issues:** P2-15, P2-35
- **why together:** One convention, settled once. P2-35's three unfixed date-range forms (contracts, resource-requests, both project-plans work-package forms) are the first consumers of P2-15's primitive: they already carry the endNotBeforeStart group validator and a Save disabled on invalid, and need exactly the aria-invalid/aria-describedby/inline-message contract P2-15 defines — projects.ts:193-197 is the in-tree model for both. The primitive must also absorb the two competing markups already in the tree (command-field-error at 8 sites vs hand-rolled <p role="alert" class="mt-1 text-xs text-critical-text"> at 6), which is a decision, not a detail.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/shared/form-field.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/commercial/contracts/contracts.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/project-plans/project-plans.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/resource-requests/resource-requests.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/projects/projects/projects.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/configuration/manage-project-roles.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/resources/resources.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/styles.css, /Users/gabriele.rendina/tools/project-resource-mgmt/scripts/smoke-noauth.mjs
- **risk:** Wrapping 31 forms' controls can break formControlName binding and the error copy that projects.spec.ts and resources.component.spec.ts already render — those two suites are the only existing detectors. Nothing asserts the endBeforeStart WIRING in contracts, project-plans or resource-requests (projects.spec.ts does not reference it either), so the {validators: endNotBeforeStart(...)} option can be dropped silently today; each form needs a real-form validity assertion plus the equal-dates case so an over-rejecting fix also goes red, and the message assertion must use equality ('End date is required.' satisfies toContain('date')). Note the visual change is larger than the diff: styles.css:965-973 already keys a red border off .command-input[aria-invalid="true"], so 24 forms light up the first time the attribute is set. The scaling detector is a static guard ([disabled]="…invalid" without an aria-invalid in the same file), not 31 DOM specs.

### B16. Multi-select without Ctrl/Cmd: chips primitive

- **effort:** medium
- **issues:** P2-19
- **why together:** Alone because its failure mode is data, not markup: the three <select multiple> controls carry an orphan-value contract (a stored skill/catalog/cost-centre absent from today's option list must stay selected and removable), so the new primitive's model is the raw string[] and any intersection with the option list silently drops saved values. That risk is a level above the aria/label work it would otherwise sit with. Its call sites overlap B15's (resource-requests.component.ts and the configuration forms) — land it after B15 so those templates are edited once more, not twice.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/shared/multi-select-chips.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/resource-requests/resource-requests.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/configuration/manage-skills.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/configuration/manage-resource-organizations.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/scripts/smoke-noauth.mjs
- **risk:** Silent value loss on save is the regression to fear: cost-centre lists on resource organizations feed cost allocation and request skills drive matching. None of the three screens has a spec today, so nothing would notice. The detector that cannot drift is the NEGATIVE assertion — after the swap querySelector('select[multiple]') must be null, since chips rendered alongside a surviving select pass any positive-only check — plus an orphan-value case (control ['Java','LegacySkill'] against options ['Java','JavaScript']) whose value ends up exactly ['LegacySkill'], and a static guard that no <select … multiple> remains under src/app. Copy the choose-then-add + removable-chip pattern from my-profile.component.ts:136-175 rather than inventing one, and give each remove button an aria-label.

### B17. Small a11y and copy fixes on interactive controls

- **effort:** small
- **issues:** P2-09, P2-14, P2-20, P3-01
- **why together:** Four template-level edits at one review depth — no mutation semantics, no money, no RBAC — held together by one convention: every interactive control must be operable by keyboard and must expose its own state. P2-09 and P3-01 are the same file (approvals.ts, and P3-01's Italian placeholder sits on a control whose aria-label is already English, so the two labels for one input disagree); P2-09 also edits resource-requests.component.ts. Where the two P2-14 findings disagree about the test, believe the one that says jsdom loads no stylesheet: assert classList.contains('sr-only') and !contains('hidden'), because getComputedStyle(input).display is '' for both and would pass on today's broken markup.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/approvals/approvals.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/approvals/approvals.spec.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/resource-requests/resource-requests.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/my-profile/my-profile.component.ts, /Users/gabriele.rendina/tools/project-resource-mgmt/src/app/my-assignments/my-assignments.component.ts
- **risk:** P2-20 is the only member that changes a model: month cells stop truncating to two assignments, so the month view renders every booking and must not be clipped by the min-h-[112px] cell inside the overflow-hidden grid — expand into a panel below the grid or the existing appModal directive, and keep roundHours' 2-decimal output. my-assignments.component.spec.ts and my-profile.component.spec.ts exist as harnesses but assert none of this; approvals.spec.ts:133-137 already builds the row that renders the note input, so the placeholder and aria-pressed assertions have a fixture waiting. Assert the aria-pressed PAIR ['true','false'] and the placeholder with toBe — 'Nota' contains 'Not', and getAttribute('aria-pressed') is truthy even when it is the string 'false'. For P2-20 the load-bearing assertion is the third assignment's name appearing only after activation, not the '+1 more' string, which is present on today's dead <div>. resource-requests.component.ts is also edited by B15/B16.

### B18. Toolchain gates: CI pipeline and Node pin enforcement

- **effort:** large
- **issues:** P2-33, P3-04
- **why together:** P3-04's only remaining clause IS P2-33's scope — there is no pipeline in which to pin the Node version — so the two are one decision: .npmrc engine-strict (plus a volta key) for the local install, a Node 22 workflow for the shared one. They also share the highest-yield step by far: turning scripts/smoke-api.mjs and scripts/smoke-noauth.mjs from manual scripts into gates that actually run, which converts two real existing checks into enforcement without writing a single new assertion.
- **files:** /Users/gabriele.rendina/tools/project-resource-mgmt/.github/workflows/ci.yml, /Users/gabriele.rendina/tools/project-resource-mgmt/.npmrc, /Users/gabriele.rendina/tools/project-resource-mgmt/package.json, /Users/gabriele.rendina/tools/project-resource-mgmt/angular.json, /Users/gabriele.rendina/tools/project-resource-mgmt/scripts/smoke-noauth.mjs, /Users/gabriele.rendina/tools/project-resource-mgmt/scripts/smoke-api.mjs
- **risk:** Two live hazards. First, engine-strict=true makes npm install exit non-zero for anyone on Node ≠22 — this machine reports v25.2.1 and other worktrees are mid-run, so that file lands only with the user's agreement and after they switch; verify with npm install --dry-run before and after, since no unit test can observe an install-time gate. Second and worse for this repo: a pipeline assumed green rather than proven red. Demonstrate it failing on three injected faults, one at a time (remove an rxResource's params → smoke-noauth part A red; add text-white to a bg-accent element → B6's guard red; delete a th's scope → B5's guard red) and keep the transcript in the PR. Floor any coverage threshold at the measured current number so it can only ratchet. State in the PR what CI still will not catch: scripts/smoke-noauth.mjs:34-45 records that its SSR pass is blind on any screen that handles a read error gracefully. Also add a three-way pin-consistency assertion (.nvmrc major === engines.node major === Dockerfile FROM node: tag) — nothing keeps those three literals in sync today.

